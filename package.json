{
  "name": "agridecision-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AgriDecision AI — Agricultural Intelligence Platform Monorepo",
  "workspaces": [
    "frontend/apps/*",
    "frontend/packages/*",
    "mobile"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
